---

---

<header class="site-header">
  <div class="utility-bar">
    <div class="utility-bar__inner">
      <p class="utility-bar__message">
        Compassionate Mental Health Care — Call Us Now:
        <a href="tel:7868267552" class="utility-bar__phone">786-826-7552</a>
      </p>
    </div>
  </div>

  <nav class="nav" aria-label="Main navigation">
    <div class="nav__inner">
      <button
        class="nav__toggle"
        aria-label="Toggle navigation menu"
        aria-expanded="false"
        aria-controls="nav-menu"
      >
        <span class="nav__toggle-bar"></span>
        <span class="nav__toggle-bar"></span>
        <span class="nav__toggle-bar"></span>
      </button>

      <div class="nav__menu" id="nav-menu">
        <ul class="nav__list nav__list--left" role="list">
          <li class="nav__item">
            <a href="/" class="nav__link" aria-current="page">Home</a>
          </li>
          <li class="nav__item">
            <a href="/residency" class="nav__link">Residency</a>
          </li>
          <li class="nav__item">
            <a href="/programs" class="nav__link">Programs</a>
          </li>
          <li class="nav__item">
            <a href="/admissions" class="nav__link">Admissions</a>
          </li>
        </ul>

        <a href="/" class="nav__logo nav__logo--desktop" aria-label="Southern Winds Hospital — Home">
          <img src="/images/logo.svg" style="width: 150px" />
        </a>

        <ul class="nav__list nav__list--right" role="list">
          <li class="nav__item">
            <a href="/what-we-treat" class="nav__link">What We Treat</a>
          </li>
          <li class="nav__item">
            <a href="/insurance" class="nav__link">Insurance</a>
          </li>
          <li class="nav__item">
            <a href="/careers" class="nav__link">Careers</a>
          </li>
          <li class="nav__item">
            <a href="/contact" class="nav__cta">Contact Us</a>
          </li>
        </ul>
      </div>

      <a href="/" class="nav__logo nav__logo--mobile" aria-label="Southern Winds Hospital — Home">
        <img src="/images/logo.svg" style="width: 100px" />
      </a>
    </div>
  </nav>
</header>

<style lang="scss">
  @use '../styles/partials/variables' as *;
  @use '../styles/partials/mixins' as *;

  .utility-bar {
    background-color: $color-navy;
    padding: $space-sm 0;

    &__inner {
      @include container;
      display: flex;
      justify-content: center;
      align-items: center;
    }

    &__message {
      font-family: $font-body;
      font-size: $fs-xs;
      font-weight: 500;
      color: rgba($color-white, 0.85);
      letter-spacing: 0.03em;

      @include respond(md) {
        font-size: $fs-sm;
      }
    }

    &__phone {
      color: $color-white;
      font-weight: 700;
      margin-left: $space-xs;
      transition: color $transition-fast;

      &:hover {
        color: $color-accent;
      }
    }
  }

  .nav {
    position: sticky;
    top: 0;
    z-index: 1000;
    background-color: $color-white;
    border-bottom: 1px solid $color-border;
    transition: box-shadow $transition-base;

    &.is-scrolled {
      box-shadow: $shadow-md;
    }

    &__inner {
      @include container;
      display: flex;
      align-items: center;
      justify-content: space-between;
      height: 72px;

      @include respond(lg) {
        height: 80px;
        justify-content: center;
      }
    }

    &__logo {
      display: flex;
      align-items: center;
      text-decoration: none;
      flex-shrink: 0;

      &--desktop {
        display: none;

        @include respond(lg) {
          display: flex;
          margin: 0 $space-lg;
        }
      }

      &--mobile {
        display: flex;

        @include respond(lg) {
          display: none;
        }
      }
    }

    &__toggle {
      display: flex;
      flex-direction: column;
      justify-content: center;
      gap: 5px;
      width: 32px;
      height: 32px;
      padding: 4px;

      @include respond(lg) {
        display: none;
      }
    }

    &__toggle-bar {
      display: block;
      width: 100%;
      height: 2px;
      background-color: $color-navy;
      border-radius: 2px;
      transition: all $transition-base;
    }

    &__menu {
      display: none;
      position: absolute;
      top: 100%;
      left: 0;
      right: 0;
      background-color: $color-white;
      border-bottom: 1px solid $color-border;
      padding: $space-lg;
      box-shadow: $shadow-lg;

      &.is-open {
        display: block;
      }

      @include respond(lg) {
        display: flex;
        position: static;
        align-items: center;
        background: none;
        border: none;
        padding: 0;
        box-shadow: none;
        width: 100%;
        justify-content: center;
      }
    }

    &__list {
      display: flex;
      flex-direction: column;
      gap: $space-xs;

      @include respond(lg) {
        flex-direction: row;
        align-items: center;
        gap: 0;
        flex: 1;
      }

      &--left {
        @include respond(lg) {
          justify-content: flex-end;
        }
      }

      &--right {
        @include respond(lg) {
          justify-content: flex-start;
        }
      }
    }

    &__link {
      display: block;
      padding: $space-sm $space-md;
      font-family: $font-body;
      font-size: $fs-sm;
      font-weight: 500;
      color: $color-gray-700;
      letter-spacing: 0.01em;
      border-radius: $radius-sm;
      transition: all $transition-fast;
      white-space: nowrap;

      &:hover,
      &[aria-current="page"] {
        color: $color-primary;
        background-color: rgba($color-primary, 0.05);
      }

      &[aria-current="page"] {
        font-weight: 600;
      }
    }

    &__cta {
      @include btn-primary;
      margin-top: $space-md;
      justify-content: center;

      @include respond(lg) {
        margin-top: 0;
        margin-left: $space-sm;
      }
    }
  }
</style>

<script>
  const toggle = document.querySelector('.nav__toggle');
  const menu = document.querySelector('.nav__menu');
  const nav = document.querySelector('.nav');

  if (toggle && menu) {
    toggle.addEventListener('click', () => {
      const expanded = toggle.getAttribute('aria-expanded') === 'true';
      toggle.setAttribute('aria-expanded', String(!expanded));
      menu.classList.toggle('is-open');
    });
  }

  if (nav) {
    window.addEventListener('scroll', () => {
      nav.classList.toggle('is-scrolled', window.scrollY > 10);
    }, { passive: true });
  }
</script>